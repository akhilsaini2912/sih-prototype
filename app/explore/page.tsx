'use client'
import { useMemo, useState } from 'react'
import Link from 'next/link'
import { Heart, MapPin, Search } from 'lucide-react'
import { SiteShell } from '@/components/site-shell'
import { places } from '@/lib/travel-data'
export default function ExplorePage() { const [query, setQuery] = useState(''); const [saved, setSaved] = useState<string[]>([]); const filtered = useMemo(() => places.filter((place) => `${place.name} ${place.location} ${place.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())), [query]); return <SiteShell><section className="mx-auto max-w-7xl px-5 py-10 lg:px-10 lg:py-16"><p className="font-mono text-xs font-bold uppercase tracking-[0.24em] text-primary">Explore India differently</p><h1 className="mt-3 font-serif text-5xl font-semibold">Find your kind of place.</h1><div className="mt-7 flex max-w-2xl items-center gap-3 rounded-2xl border border-border bg-card px-4 py-3 shadow-sm"><Search className="size-5 text-primary" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Try quiet Kerala, crafts, or low crowd" className="w-full bg-transparent text-sm outline-none" /></div><div className="mt-8 grid gap-5 md:grid-cols-2">{filtered.map((place) => <article key={place.id} className="overflow-hidden rounded-3xl border border-border bg-card p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold text-primary">{place.type}</p><h2 className="mt-2 font-serif text-2xl font-semibold">{place.name}</h2><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><MapPin className="size-3.5" />{place.location}</p></div><button onClick={() => setSaved((current) => current.includes(place.id) ? current.filter((id) => id !== place.id) : [...current, place.id])} aria-label="Save experience" className="rounded-full bg-secondary p-2"><Heart className={`size-4 ${saved.includes(place.id) ? 'fill-current text-primary' : ''}`} /></button></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{place.description}</p><div className="mt-5 flex items-center justify-between"><span className="text-xs font-semibold text-chart-2">{place.crowd} crowd · {place.price}</span><Link href="/trips" className="rounded-full bg-primary px-4 py-2 text-xs font-bold text-primary-foreground">Add to trip</Link></div></article>)}</div></section></SiteShell> }
