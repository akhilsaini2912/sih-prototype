'use client'

import { useMemo, useState } from 'react'
import Link from 'next/link'
import { ArrowRight, BedDouble, Check, ChevronDown, CircleDollarSign, Coffee, MapPin, Sparkles, Utensils } from 'lucide-react'
import { SiteShell } from '@/components/site-shell'

const days = {
  2: ['Amber Fort sunrise + heritage thali', 'Hawa Mahal photo walk + old city food trail'],
  3: ['Amber Fort sunrise + heritage thali', 'Hawa Mahal, City Palace + block printing workshop', 'Albert Hall sunset + ghewar tasting'],
  4: ['Amber Fort sunrise + heritage thali', 'Hawa Mahal, City Palace + block printing workshop', 'Albert Hall sunset + ghewar tasting', 'Nahargarh sunrise + artisan market'],
}

const stays = [{ name: 'Raj Palace Heritage Stay', price: '₹2,100/night', match: '94%', detail: 'Heritage character, central route, breakfast included' }, { name: 'Moti Courtyard Homestay', price: '₹1,350/night', match: '91%', detail: 'Local host, quiet lane, home-cooked dinner' }, { name: 'Pink City Smart Hotel', price: '₹2,750/night', match: '86%', detail: 'Pool, workspace, easy airport transfer' }]
const dishes = [{ name: 'Dal Baati Churma', price: '₹180–₹320', place: 'Surya Mahal Kitchen' }, { name: 'Pyaaz Kachori', price: '₹35–₹70', place: 'Rawat Misthan Bhandar' }, { name: 'Ghewar & Lassi', price: '₹120–₹240', place: 'Laxmi Mishthan Bhandar' }]

export default function PlanPage() {
  const [duration, setDuration] = useState(3)
  const [budget, setBudget] = useState('₹8,000')
  const [travellers, setTravellers] = useState(2)
  const [generated, setGenerated] = useState(false)
  const itinerary = useMemo(() => days[duration as keyof typeof days], [duration])
  return <SiteShell><main className="mx-auto max-w-7xl px-5 py-10 lg:px-10 lg:py-14"><div className="max-w-3xl"><p className="font-mono text-xs font-bold uppercase tracking-[0.24em] text-primary">AI journey builder</p><h1 className="mt-3 font-serif text-5xl font-semibold leading-[1.04] tracking-tight sm:text-6xl">A Jaipur plan that knows your days, budget and appetite.</h1><p className="mt-5 text-base leading-7 text-muted-foreground">Create a demo itinerary with transparent market prices for stays, transport and Rajasthan&apos;s most-loved dishes. Every number is sample data for the prototype.</p></div><section className="mt-10 grid gap-6 lg:grid-cols-[.78fr_1.22fr]"><div className="rounded-[2rem] border border-border bg-card p-5 shadow-sm sm:p-7"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-2xl bg-accent/50"><Sparkles className="size-5 text-primary" /></span><div><h2 className="font-serif text-2xl font-semibold">Tell Yatra the shape</h2><p className="text-xs text-muted-foreground">We&apos;ll build the first useful version.</p></div></div><label className="mt-7 block text-sm font-bold">Destination<input value="Jaipur, Rajasthan" readOnly className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm outline-none" /></label><label className="mt-4 block text-sm font-bold">Days<select value={duration} onChange={(e) => setDuration(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm outline-none"><option value={2}>2 days</option><option value={3}>3 days</option><option value={4}>4 days</option></select></label><label className="mt-4 block text-sm font-bold">Travellers<input type="number" min={1} max={8} value={travellers} onChange={(e) => setTravellers(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm outline-none" /></label><label className="mt-4 block text-sm font-bold">Total budget<input value={budget} onChange={(e) => setBudget(e.target.value)} className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm outline-none" /></label><div className="mt-5 flex flex-wrap gap-2">{['Heritage','Food','Culture','Shopping'].map((item, i) => <button key={item} className={`rounded-full px-3 py-2 text-xs font-bold ${i < 3 ? 'bg-primary text-primary-foreground' : 'border border-border text-muted-foreground'}`}>{item}</button>)}</div><button onClick={() => setGenerated(true)} className="mt-7 flex w-full items-center justify-center gap-2 rounded-2xl bg-primary px-5 py-3.5 text-sm font-bold text-primary-foreground shadow-lg hover:-translate-y-0.5">Generate my {duration}-day journey <ArrowRight className="size-4" /></button></div><div className="rounded-[2rem] border border-border bg-card p-5 shadow-sm sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.2em] text-primary">{generated ? 'Journey ready' : 'Preview itinerary'}</p><h2 className="mt-2 font-serif text-3xl font-semibold">Jaipur in {duration} days</h2><p className="mt-1 text-sm text-muted-foreground">{travellers} travellers · {budget} working budget · balanced pace</p></div><span className="rounded-full bg-accent/50 px-3 py-2 text-xs font-bold text-primary">91 journey score</span></div><div className="mt-7 flex flex-col gap-3">{itinerary.map((item, index) => <div key={item} className="flex gap-4 rounded-2xl bg-secondary/60 p-4"><span className="flex size-8 shrink-0 items-center justify-center rounded-xl bg-primary text-xs font-bold text-primary-foreground">{index + 1}</span><div><p className="font-semibold">Day {index + 1}</p><p className="mt-1 text-sm text-muted-foreground">{item}</p><p className="mt-2 text-xs text-primary">₹{index === 0 ? '1,120' : index === 1 ? '980' : '760'} estimated · 18–35 min transfer</p></div></div>)}</div><div className="mt-5 flex items-center gap-2 rounded-2xl border border-primary/20 bg-accent/25 p-4 text-sm"><Check className="size-4 text-primary" /> Why Yatra recommends this: heritage + food fit, low-crowd timing, and local businesses within your budget.</div></div></section><section className="mt-8 grid gap-6 lg:grid-cols-2"><div className="rounded-[2rem] border border-border bg-card p-5 sm:p-7"><div className="flex items-center gap-3"><BedDouble className="size-5 text-primary" /><h2 className="font-serif text-2xl font-semibold">Stay that fits the route</h2></div><div className="mt-5 flex flex-col gap-3">{stays.map((stay) => <div key={stay.name} className="rounded-2xl border border-border p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{stay.name}</p><p className="mt-1 text-xs text-muted-foreground">{stay.detail}</p></div><span className="rounded-full bg-accent/50 px-2.5 py-1 text-xs font-bold text-primary">{stay.match} match</span></div><div className="mt-3 flex items-center justify-between"><span className="text-sm font-bold">{stay.price}</span><Link href="/trips" className="text-xs font-bold text-primary">Add to journey <ArrowRight className="ml-1 inline size-3" /></Link></div></div>)}</div></div><div className="rounded-[2rem] border border-border bg-card p-5 sm:p-7"><div className="flex items-center gap-3"><Utensils className="size-5 text-primary" /><h2 className="font-serif text-2xl font-semibold">Eat like a local</h2></div><p className="mt-1 text-sm text-muted-foreground">Market amount estimates, so you can choose confidently.</p><div className="mt-5 flex flex-col gap-3">{dishes.map((dish) => <div key={dish.name} className="flex items-center justify-between gap-3 rounded-2xl bg-secondary/60 p-4"><div><p className="font-semibold">{dish.name}</p><p className="mt-1 text-xs text-muted-foreground">{dish.place}</p></div><span className="text-right text-xs font-bold text-primary">{dish.price}</span></div>)}</div><div className="mt-5 flex items-center gap-2 text-xs text-muted-foreground"><CircleDollarSign className="size-4 text-primary" /> Food trail estimate: ₹350–₹650 per person/day</div></div></section><section className="mt-8 rounded-[2rem] border border-border bg-secondary/45 p-5 sm:p-7"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.2em] text-primary">One workspace</p><h2 className="mt-2 font-serif text-3xl font-semibold">Ready to make it yours?</h2></div><Link href="/trips" className="rounded-full bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Open My Journey <ArrowRight className="ml-2 inline size-4" /></Link></div></section></main></SiteShell>
}
