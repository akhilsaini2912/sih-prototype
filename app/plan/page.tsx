'use client'

import { useMemo, useState } from 'react'
import Link from 'next/link'
import { BedDouble, Check, CircleDollarSign, Map, MapPin, Save, Sparkles, Utensils } from 'lucide-react'
import { SiteShell } from '@/components/site-shell'

const plans: Record<number, string[]> = { 2: ['Amber Fort sunrise + heritage thali', 'Hawa Mahal photo walk + old city food trail'], 3: ['Amber Fort sunrise + heritage thali', 'Hawa Mahal, City Palace + block printing workshop', 'Albert Hall sunset + ghewar tasting'], 4: ['Amber Fort sunrise + heritage thali', 'Hawa Mahal, City Palace + block printing workshop', 'Albert Hall sunset + ghewar tasting', 'Nahargarh sunrise + artisan market'] }
const stays = [{ name: 'Raj Palace Heritage Stay', price: '₹2,100/night', match: '94%', detail: 'Heritage character, central route, breakfast included' }, { name: 'Moti Courtyard Homestay', price: '₹1,350/night', match: '91%', detail: 'Local host, quiet lane, home-cooked dinner' }, { name: 'Pink City Smart Hotel', price: '₹2,750/night', match: '86%', detail: 'Pool, workspace, easy airport transfer' }]
const dishes = [{ name: 'Dal Baati Churma', price: '₹180–₹320', place: 'Surya Mahal Kitchen' }, { name: 'Pyaaz Kachori', price: '₹35–₹70', place: 'Rawat Misthan Bhandar' }, { name: 'Ghewar & Lassi', price: '₹120–₹240', place: 'Laxmi Mishthan Bhandar' }]

export default function PlanPage() {
  const [duration, setDuration] = useState(3), [travellers, setTravellers] = useState(2), [budget, setBudget] = useState('₹8,000'), [saved, setSaved] = useState('')
  const itinerary = useMemo(() => plans[duration], [duration])
  async function saveJourney() { const response = await fetch('/api/journeys', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ destination: 'Jaipur, Rajasthan', duration, travellers, budget, itinerary }) }); const data = await response.json(); setSaved(data.storage === 'mongodb' ? 'Saved to your MongoDB journey vault.' : 'Preview saved locally. Connect MongoDB Atlas to persist it.') }
  return <SiteShell><main className="mx-auto max-w-7xl px-5 py-8 lg:px-10 lg:py-14"><div className="grid gap-8 lg:grid-cols-[.8fr_1.2fr] lg:items-end"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.24em] text-primary">AI journey builder</p><h1 className="mt-3 font-serif text-5xl font-semibold leading-[1.04] sm:text-6xl">Plan Jaipur by days, budget and appetite.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-muted-foreground">Choose your trip shape and Yatra returns a transparent plan with market-aware prices, hotel options, signature food and a route you can open on a map.</p></div><img src="/kerala-experience.png" alt="Quiet Indian backwater travel experience" className="h-52 w-full rounded-[2rem] object-cover lg:h-64" /></div><section className="mt-10 grid gap-6 lg:grid-cols-[.78fr_1.22fr]"><div className="rounded-[2rem] border border-border bg-card p-5 shadow-sm sm:p-7"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-2xl bg-accent/50"><Sparkles className="size-5 text-primary" /></span><div><h2 className="font-serif text-2xl font-semibold">Shape your journey</h2><p className="text-xs text-muted-foreground">The API-ready trip brief.</p></div></div><label className="mt-7 block text-sm font-bold">Destination<input readOnly value="Jaipur, Rajasthan" className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm" /></label><label className="mt-4 block text-sm font-bold">Days<select value={duration} onChange={(e) => setDuration(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm"><option value={2}>2 days</option><option value={3}>3 days</option><option value={4}>4 days</option></select></label><label className="mt-4 block text-sm font-bold">Travellers<input type="number" min={1} max={8} value={travellers} onChange={(e) => setTravellers(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm" /></label><label className="mt-4 block text-sm font-bold">Trip budget<input value={budget} onChange={(e) => setBudget(e.target.value)} className="mt-2 w-full rounded-2xl border border-border bg-secondary/50 px-4 py-3 text-sm" /></label><button onClick={saveJourney} className="mt-6 flex w-full items-center justify-center gap-2 rounded-full bg-primary px-5 py-3 text-sm font-bold text-primary-foreground"><Save className="size-4" /> Save journey</button>{saved && <p className="mt-3 rounded-xl bg-accent/40 p-3 text-xs font-semibold text-primary">{saved}</p>}</div><div className="rounded-[2rem] border border-border bg-card p-5 shadow-sm sm:p-7"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.2em] text-primary">Your brief</p><h2 className="mt-2 font-serif text-3xl font-semibold">{duration} days in the Pink City</h2><p className="mt-2 text-sm text-muted-foreground">{travellers} travellers · {budget} working budget · designed for local discovery</p></div><Link target="_blank" href="https://www.google.com/maps/search/Jaipur+Rajasthan" className="flex items-center gap-2 rounded-full border border-border px-4 py-2 text-xs font-bold hover:bg-secondary"><Map className="size-4" /> See map</Link></div><div className="mt-6 flex flex-col gap-3">{itinerary.map((item, index) => <div key={item} className="flex gap-3 rounded-2xl bg-secondary/60 p-4"><span className="flex size-8 shrink-0 items-center justify-center rounded-full bg-primary text-xs font-bold text-primary-foreground">{index + 1}</span><div><p className="text-sm font-bold">Day {index + 1}</p><p className="mt-1 text-sm text-muted-foreground">{item}</p></div></div>)}</div><div className="mt-8 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-border p-4"><BedDouble className="size-5 text-primary" /><p className="mt-3 text-xs text-muted-foreground">Stay from</p><p className="font-bold">₹1,350/night</p></div><div className="rounded-2xl border border-border p-4"><Utensils className="size-5 text-primary" /><p className="mt-3 text-xs text-muted-foreground">Food range/day</p><p className="font-bold">₹450–₹900</p></div><div className="rounded-2xl border border-border p-4"><CircleDollarSign className="size-5 text-primary" /><p className="mt-3 text-xs text-muted-foreground">Trip confidence</p><p className="font-bold">High match</p></div></div><div className="mt-8"><h3 className="font-serif text-2xl font-semibold">Hotels with a reason</h3><div className="mt-3 grid gap-3">{stays.map((stay) => <div key={stay.name} className="flex items-center justify-between gap-4 rounded-2xl border border-border p-4"><div><p className="text-sm font-bold">{stay.name}</p><p className="mt-1 text-xs text-muted-foreground">{stay.detail}</p></div><div className="text-right"><p className="text-sm font-bold">{stay.price}</p><p className="text-xs text-primary">{stay.match} match</p></div></div>)}</div></div><div className="mt-8"><h3 className="font-serif text-2xl font-semibold">What to eat</h3><div className="mt-3 grid gap-3">{dishes.map((dish) => <div key={dish.name} className="flex items-center justify-between rounded-2xl bg-accent/25 p-4"><div><p className="text-sm font-bold">{dish.name}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><MapPin className="size-3" />{dish.place}</p></div><p className="text-sm font-bold">{dish.price}</p></div>)}</div></div></div></section></main></SiteShell>
}
