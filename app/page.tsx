'use client'

import { useEffect, useMemo, useState } from 'react'
import { ArrowRight, Bot, CalendarDays, Check, ChevronRight, Compass, Heart, Leaf, MapPin, Menu, MessageCircle, Navigation, Search, Send, Sparkles, Users, X } from 'lucide-react'

type Page = 'home' | 'explore' | 'trip' | 'locals' | 'companion'

type Result = { id: string; name: string; location: string; type: string; description: string; tags: string[]; image: string; crowd: string; price: string }

const results: Result[] = [
  { id: 'kumbalangi', name: 'Kumbalangi village walk', location: 'Kochi, Kerala', type: 'Local experience', description: 'Walk the first eco-tourism village with a resident storyteller, fishers and toddy tappers.', tags: ['quiet', 'culture', 'local host'], image: '/kerala-experience.png', crowd: 'Low', price: '₹650' },
  { id: 'mawlynnong', name: 'Mawlynnong living root bridge', location: 'Meghalaya', type: 'Nature escape', description: 'A forest trail through ancient bridges and Khasi village life, best before 9am.', tags: ['nature', 'low impact', 'guided'], image: '/kerala-experience.png', crowd: 'Low', price: '₹900' },
  { id: 'blue-city', name: 'Blue City craft morning', location: 'Jodhpur, Rajasthan', type: 'Meet a local', description: 'Learn natural indigo dyeing from a third-generation artisan in the old city.', tags: ['craft', 'food nearby', 'women-led'], image: '/kerala-experience.png', crowd: 'Moderate', price: '₹1,200' },
  { id: 'marayoor', name: 'Marayoor sandalwood trail', location: 'Idukki, Kerala', type: 'Slow travel', description: 'A shaded village route with jaggery makers, sandalwood groves and mountain views.', tags: ['sustainable', 'family friendly'], image: '/kerala-experience.png', crowd: 'Low', price: '₹780' },
]

const navItems: { id: Page; label: string }[] = [
  { id: 'home', label: 'Overview' }, { id: 'explore', label: 'Explore' }, { id: 'trip', label: 'My trip' }, { id: 'locals', label: 'Meet locals' }, { id: 'companion', label: 'AI companion' },
]

export default function Home() {
  const [page, setPage] = useState<Page>('home')
  const [menuOpen, setMenuOpen] = useState(false)
  const [query, setQuery] = useState('')
  const [searching, setSearching] = useState(false)
  const [saved, setSaved] = useState<string[]>([])
  const [tripItems, setTripItems] = useState<string[]>(['kumbalangi'])
  const [chat, setChat] = useState([{ from: 'ai', text: 'Namaste. I can help you find the right pace, place and people for your next trip.' }])
  const [message, setMessage] = useState('')

  const filtered = useMemo(() => {
    const normalized = query.toLowerCase().trim()
    if (!normalized) return results
    return results.filter((item) => `${item.name} ${item.location} ${item.type} ${item.tags.join(' ')}`.toLowerCase().includes(normalized))
  }, [query])

  useEffect(() => { if (query) setSearching(true); const timer = setTimeout(() => setSearching(false), 350); return () => clearTimeout(timer) }, [query])

  function go(next: Page) { setPage(next); setMenuOpen(false); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  function toggleSave(id: string) { setSaved((current) => current.includes(id) ? current.filter((value) => value !== id) : [...current, id]) }
  function addToTrip(id: string) { setTripItems((current) => current.includes(id) ? current : [...current, id]) }
  function sendMessage() { if (!message.trim()) return; setChat((current) => [...current, { from: 'user', text: message }, { from: 'ai', text: 'I would start with a quieter local experience, then keep a flexible window for weather and the people you meet. Want me to shape that into your trip?' }]); setMessage('') }

  return <main className="min-h-screen bg-background text-foreground">
    <header className="sticky top-0 z-40 border-b border-border/80 bg-background/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-10"><button onClick={() => go('home')} className="flex items-center gap-3 text-left"><span className="flex size-10 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><Navigation className="size-5" /></span><span><span className="block font-mono text-[10px] font-bold uppercase tracking-[0.24em] text-primary">Yatra / AI</span><span className="block font-serif text-lg font-semibold leading-none">Go beyond the map</span></span></button><nav className="hidden items-center gap-1 rounded-full border border-border bg-card p-1 md:flex">{navItems.map((item) => <button key={item.id} onClick={() => go(item.id)} className={`rounded-full px-4 py-2 text-sm font-semibold transition ${page === item.id ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:bg-secondary hover:text-foreground'}`}>{item.label}</button>)}</nav><div className="flex items-center gap-2"><button onClick={() => go('explore')} aria-label="Search travel" className="rounded-full p-2.5 text-muted-foreground hover:bg-secondary"><Search className="size-5" /></button><button onClick={() => setMenuOpen((value) => !value)} aria-label="Open menu" className="rounded-full border border-border p-2 md:hidden">{menuOpen ? <X className="size-5" /> : <Menu className="size-5" />}</button></div></div>{menuOpen && <nav className="mx-5 mb-4 flex flex-col gap-1 rounded-2xl border border-border bg-card p-2 shadow-lg md:hidden">{navItems.map((item) => <button key={item.id} onClick={() => go(item.id)} className={`rounded-xl p-3 text-left text-sm font-semibold ${page === item.id ? 'bg-primary text-primary-foreground' : 'hover:bg-secondary'}`}>{item.label}</button>)}</nav>}</header>

    {page === 'home' && <HomePage go={go} onExplore={() => go('explore')} />}
    {page === 'explore' && <ExplorePage query={query} setQuery={setQuery} filtered={filtered} searching={searching} saved={saved} toggleSave={toggleSave} addToTrip={addToTrip} tripItems={tripItems} />}
    {page === 'trip' && <TripPage tripItems={tripItems} go={go} />}
    {page === 'locals' && <LocalsPage />}
    {page === 'companion' && <CompanionPage chat={chat} message={message} setMessage={setMessage} sendMessage={sendMessage} />}

    <footer className="border-t border-border"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between lg:px-10"><p><span className="font-semibold text-foreground">Yatra AI</span> · SIH tourism prototype</p><div className="flex gap-5"><button onClick={() => go('explore')} className="hover:text-foreground">Discover</button><button onClick={() => go('locals')} className="hover:text-foreground">For local businesses</button><button onClick={() => go('companion')} className="hover:text-foreground">Help</button></div></div></footer>
  </main>
}

function HomePage({ go, onExplore }: { go: (page: Page) => void; onExplore: () => void }) { return <div><section className="mx-auto grid max-w-7xl items-center gap-12 px-5 pb-16 pt-12 lg:grid-cols-[1.02fr_.98fr] lg:px-10 lg:pb-24 lg:pt-20"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><Sparkles className="size-3.5" /> Smarter journeys. Deeper India.</div><h1 className="max-w-2xl font-serif text-5xl font-semibold leading-[1.03] tracking-tight sm:text-6xl lg:text-7xl">Travel <span className="italic text-primary">with the place,</span> not just through it.</h1><p className="mt-7 max-w-xl text-base leading-7 text-muted-foreground sm:text-lg">An adaptive AI travel companion for discovering India’s living culture, local businesses and slower journeys.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><button onClick={onExplore} className="rounded-full bg-primary px-6 py-3.5 text-sm font-bold text-primary-foreground shadow-lg">Find my kind of India <ArrowRight className="ml-2 inline size-4" /></button><button onClick={() => go('companion')} className="rounded-full border border-border bg-card px-6 py-3.5 text-sm font-bold hover:bg-secondary"><Bot className="mr-2 inline size-4 text-primary" /> Talk to Yatra</button></div></div><div className="overflow-hidden rounded-[2rem] border border-border bg-card shadow-2xl"><div className="relative h-[330px] sm:h-[430px]"><img src="/kerala-experience.png" alt="Quiet Kerala backwater village at sunrise" className="h-full w-full object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-foreground/70 via-transparent to-transparent" /><div className="absolute bottom-6 left-6 right-6 flex items-end justify-between text-primary-foreground"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] opacity-80">Featured route</p><h2 className="mt-1 font-serif text-3xl font-semibold">Kerala, unhurried.</h2></div><span className="rounded-full bg-background/20 px-3 py-1.5 text-xs font-semibold backdrop-blur">7 days · flexible</span></div></div><div className="grid grid-cols-3 divide-x divide-border"><Metric label="Local spots" value="12" /><Metric label="Crowd score" value="Low" /><Metric label="CO₂ saved" value="31%" /></div></div></section><section className="border-y border-border bg-secondary/40"><div className="mx-auto grid max-w-7xl gap-4 px-5 py-12 sm:grid-cols-3 lg:px-10"><Stat icon={Search} title="Search deeper" copy="Go beyond famous places to local-led experiences." /><Stat icon={Users} title="Meet better" copy="Connect with verified makers, hosts and businesses." /><Stat icon={Leaf} title="Travel lighter" copy="Choose routes that are kinder to people and planet." /></div></section></div> }

function ExplorePage({ query, setQuery, filtered, searching, saved, toggleSave, addToTrip, tripItems }: { query: string; setQuery: (value: string) => void; filtered: Result[]; searching: boolean; saved: string[]; toggleSave: (id: string) => void; addToTrip: (id: string) => void; tripItems: string[] }) { return <section className="mx-auto max-w-7xl px-5 py-10 lg:px-10 lg:py-14"><div className="flex flex-col justify-between gap-6 lg:flex-row lg:items-end"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.24em] text-primary">Explore India differently</p><h1 className="mt-3 font-serif text-5xl font-semibold tracking-tight">Find your kind of place.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-muted-foreground">Search destinations, hidden experiences, sustainable routes and trusted local hosts.</p></div><div className="flex items-center gap-2 rounded-full bg-accent/40 px-4 py-2 text-sm font-semibold text-accent-foreground"><Check className="size-4" /> {tripItems.length} saved to trip</div></div><div className="mt-8 flex flex-col gap-3 rounded-3xl border border-border bg-card p-3 shadow-lg sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-2xl bg-secondary px-4 py-3"><Search className="size-5 text-primary" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Try ‘quiet Kerala’, ‘crafts’, or ‘low crowd’" className="w-full bg-transparent text-sm outline-none placeholder:text-muted-foreground" /><span className="hidden rounded-lg bg-background px-2 py-1 text-[10px] text-muted-foreground sm:block">⌘ K</span></div><button className="rounded-2xl bg-primary px-6 py-3 text-sm font-bold text-primary-foreground">Search Yatra</button></div><div className="mt-8 flex flex-wrap gap-2">{['All experiences', 'Low crowd', 'Local food', 'Nature', 'Arts & crafts', 'Sustainable'].map((filter) => <button key={filter} onClick={() => setQuery(filter === 'All experiences' ? '' : filter)} className="rounded-full border border-border bg-card px-4 py-2 text-xs font-semibold hover:border-primary hover:text-primary">{filter}</button>)}</div><div className="mt-10 flex items-center justify-between"><p className="text-sm text-muted-foreground">{searching ? 'Searching the Yatra index…' : `${filtered.length} thoughtful places found`}</p><button className="flex items-center gap-1 text-sm font-semibold text-primary">Sort by fit <ChevronRight className="size-4" /></button></div><div className="mt-5 grid gap-5 md:grid-cols-2">{filtered.map((item) => <ResultCard key={item.id} item={item} saved={saved.includes(item.id)} inTrip={tripItems.includes(item.id)} onSave={() => toggleSave(item.id)} onAdd={() => addToTrip(item.id)} />)}</div>{filtered.length === 0 && <div className="rounded-3xl border border-dashed border-border p-12 text-center"><Search className="mx-auto size-8 text-muted-foreground" /><h2 className="mt-4 font-serif text-2xl font-semibold">No exact match yet</h2><p className="mt-2 text-sm text-muted-foreground">Try Kerala, local, nature, craft or low crowd.</p></div>}</section> }

function ResultCard({ item, saved, inTrip, onSave, onAdd }: { item: Result; saved: boolean; inTrip: boolean; onSave: () => void; onAdd: () => void }) { return <article className="overflow-hidden rounded-3xl border border-border bg-card shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="relative h-48"><img src={item.image} alt={item.name} className="h-full w-full object-cover" /><span className="absolute left-4 top-4 rounded-full bg-background/85 px-3 py-1 text-[10px] font-bold backdrop-blur">{item.type}</span><button onClick={onSave} aria-label={saved ? 'Remove from saved' : 'Save experience'} className={`absolute right-4 top-4 rounded-full p-2.5 backdrop-blur ${saved ? 'bg-accent text-accent-foreground' : 'bg-background/85'}`}><Heart className={`size-4 ${saved ? 'fill-current' : ''}`} /></button></div><div className="p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-serif text-2xl font-semibold">{item.name}</h2><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><MapPin className="size-3.5" /> {item.location}</p></div><span className="font-semibold text-primary">{item.price}</span></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{item.description}</p><div className="mt-4 flex flex-wrap gap-2">{item.tags.map((tag) => <span key={tag} className="rounded-full bg-secondary px-2.5 py-1 text-[10px] font-semibold text-muted-foreground">{tag}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-border pt-4"><span className="text-xs font-semibold text-chart-2">{item.crowd} crowd today</span><button onClick={onAdd} disabled={inTrip} className="rounded-full bg-primary px-4 py-2 text-xs font-bold text-primary-foreground disabled:cursor-default disabled:opacity-60">{inTrip ? 'Added to trip' : 'Add to trip'}</button></div></div></article> }

function TripPage({ tripItems, go }: { tripItems: string[]; go: (page: Page) => void }) { const items = results.filter((item) => tripItems.includes(item.id)); return <section className="mx-auto max-w-7xl px-5 py-10 lg:px-10 lg:py-14"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.24em] text-primary">Your intelligent itinerary</p><h1 className="mt-3 font-serif text-5xl font-semibold">Kerala, unhurried.</h1><p className="mt-3 text-sm text-muted-foreground">14–20 October · Slow & soulful · Flexible pace</p></div><button onClick={() => go('explore')} className="w-fit rounded-full border border-border px-4 py-2.5 text-sm font-semibold hover:bg-secondary">Add another stop</button></div><div className="mt-8 grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><div className="rounded-3xl border border-border bg-card p-5 sm:p-7"><div className="flex items-center justify-between border-b border-border pb-5"><div><p className="font-serif text-2xl font-semibold">Tuesday, 14 October</p><p className="mt-1 text-xs text-muted-foreground">Kochi · 28°C · Low crowd forecast</p></div><CalendarDays className="size-5 text-primary" /></div><div className="mt-6 flex flex-col gap-3">{items.map((item, index) => <div key={item.id} className="flex gap-4 rounded-2xl bg-secondary/55 p-4"><div className="font-mono text-xs font-bold text-muted-foreground">{`0${index + 9}:00`}</div><div className="mt-1 size-2.5 shrink-0 rounded-full bg-primary" /><div className="flex-1"><div className="flex flex-wrap justify-between gap-2"><p className="font-semibold">{item.name}</p><span className="text-xs font-semibold text-chart-2">{item.crowd} crowd</span></div><p className="mt-1 text-xs text-muted-foreground">{item.type} · {item.location}</p></div></div>)}</div><div className="mt-5 flex items-start gap-3 rounded-2xl bg-accent/35 p-4"><Sparkles className="mt-0.5 size-4 text-accent-foreground" /><p className="text-xs leading-5"><strong>AI suggestion:</strong> Keep 90 minutes open after lunch. A local monsoon craft workshop just opened nearby.</p></div></div><aside className="rounded-3xl bg-primary p-6 text-primary-foreground"><p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-accent">Trip health</p><h2 className="mt-3 font-serif text-3xl">Good balance.</h2><div className="mt-8 flex flex-col gap-5">{[['Crowd comfort', 'Low', '82%'], ['Local spend', 'Strong', '68%'], ['Impact saved', '31%', '31%']].map(([label, value, width]) => <div key={label}><div className="flex justify-between text-xs"><span className="text-primary-foreground/70">{label}</span><strong>{value}</strong></div><div className="mt-2 h-2 rounded-full bg-primary-foreground/15"><div className="h-full rounded-full bg-accent" style={{ width }} /></div></div>)}</div><button onClick={() => go('companion')} className="mt-9 w-full rounded-full bg-accent py-3 text-sm font-bold text-accent-foreground">Ask Yatra to replan</button></aside></div></section> }

function LocalsPage() { const locals = [{ name: 'Babu K.', role: 'Toddy tapping & village walks', place: 'Kumbalangi, Kochi', rating: '4.9', color: 'bg-primary' }, { name: 'Meera S.', role: 'Indigo craft workshop', place: 'Jodhpur, Rajasthan', rating: '4.8', color: 'bg-accent' }, { name: 'Lynette R.', role: 'Khasi food & forest guide', place: 'Shillong, Meghalaya', rating: '5.0', color: 'bg-chart-2' }]; return <section className="mx-auto max-w-7xl px-5 py-10 lg:px-10 lg:py-14"><p className="font-mono text-xs font-bold uppercase tracking-[0.24em] text-primary">Tourist ↔ local matching</p><h1 className="mt-3 max-w-2xl font-serif text-5xl font-semibold tracking-tight">The people are part of the place.</h1><p className="mt-4 max-w-xl text-sm leading-6 text-muted-foreground">Meet verified local hosts, makers and businesses whose stories make your trip more meaningful.</p><div className="mt-10 grid gap-5 md:grid-cols-3">{locals.map((local) => <article key={local.name} className="rounded-3xl border border-border bg-card p-6 shadow-sm"><div className={`flex size-14 items-center justify-center rounded-2xl ${local.color} font-serif text-2xl ${local.color === 'bg-accent' ? 'text-accent-foreground' : 'text-primary-foreground'}`}>{local.name.charAt(0)}</div><h2 className="mt-5 font-serif text-2xl font-semibold">{local.name}</h2><p className="mt-1 text-sm font-semibold text-primary">{local.role}</p><p className="mt-2 text-xs text-muted-foreground">{local.place}</p><div className="mt-5 flex items-center justify-between border-t border-border pt-4"><span className="text-xs font-semibold">{local.rating} / 5 · Verified</span><button className="rounded-full bg-primary px-3 py-2 text-xs font-bold text-primary-foreground">View profile</button></div></article>)}</div><div className="mt-8 rounded-3xl bg-secondary p-6 sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><h2 className="font-serif text-3xl font-semibold">Are you a local business?</h2><p className="mt-2 max-w-lg text-sm text-muted-foreground">Bring your experience to thoughtful travelers and grow with Yatra.</p></div><button className="w-fit rounded-full bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Join the network <ArrowRight className="ml-1 inline size-4" /></button></div></div></section> }

function CompanionPage({ chat, message, setMessage, sendMessage }: { chat: { from: string; text: string }[]; message: string; setMessage: (value: string) => void; sendMessage: () => void }) { return <section className="mx-auto max-w-5xl px-5 py-10 lg:px-10 lg:py-14"><div className="mx-auto max-w-2xl text-center"><div className="mx-auto flex size-14 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><Bot className="size-7" /></div><p className="mt-5 font-mono text-xs font-bold uppercase tracking-[0.24em] text-primary">Your AI tourism companion</p><h1 className="mt-3 font-serif text-5xl font-semibold">Ask the place, not just the internet.</h1><p className="mt-4 text-sm leading-6 text-muted-foreground">Get contextual advice on etiquette, food, weather, translations and the next best moment.</p></div><div className="mx-auto mt-10 max-w-2xl overflow-hidden rounded-3xl border border-border bg-card shadow-xl"><div className="flex items-center gap-3 border-b border-border p-5"><span className="flex size-9 items-center justify-center rounded-xl bg-accent text-accent-foreground"><Sparkles className="size-4" /></span><div><p className="text-sm font-bold">Yatra companion</p><p className="text-xs text-chart-2">Online · understands local context</p></div></div><div className="flex min-h-80 flex-col gap-4 p-5 sm:p-7">{chat.map((item, index) => <div key={`${item.text}-${index}`} className={`max-w-[85%] rounded-2xl px-4 py-3 text-sm leading-6 ${item.from === 'user' ? 'self-end bg-primary text-primary-foreground' : 'bg-secondary'}`}>{item.text}</div>)}</div><div className="border-t border-border p-4"><div className="flex items-center gap-2 rounded-2xl bg-secondary px-4 py-2"><input value={message} onChange={(event) => setMessage(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter' && !event.nativeEvent.isComposing) sendMessage() }} placeholder="Ask about your next day…" className="w-full bg-transparent py-2 text-sm outline-none" /><button onClick={sendMessage} aria-label="Send message" className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Send className="size-4" /></button></div><div className="mt-3 flex flex-wrap gap-2">{['What should I eat in Kochi?', 'Avoid crowds tomorrow', 'Translate this for me'].map((prompt) => <button key={prompt} onClick={() => setMessage(prompt)} className="rounded-full border border-border px-3 py-1.5 text-xs text-muted-foreground hover:border-primary hover:text-primary">{prompt}</button>)}</div></div></div></section> }

function Metric({ label, value }: { label: string; value: string }) { return <div className="px-4 py-4 text-center"><p className="font-serif text-xl font-semibold text-primary">{value}</p><p className="mt-1 text-[10px] uppercase tracking-wider text-muted-foreground">{label}</p></div> }
function Stat({ icon: Icon, title, copy }: { icon: typeof Search; title: string; copy: string }) { return <div className="flex gap-3 rounded-2xl border border-border bg-card p-4"><span className="flex size-9 shrink-0 items-center justify-center rounded-xl bg-accent text-accent-foreground"><Icon className="size-4" /></span><div><p className="text-sm font-bold">{title}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{copy}</p></div></div> }
